# DEVJOO — MASTER FULL-STACK PRODUCT SPECIFICATION

You are the lead software architect, senior full-stack engineer, product designer, SEO engineer, security engineer, database designer, and QA engineer responsible for building **DevJoo**.

You must actually IMPLEMENT the product.

Do not only provide plans, pseudo-code, explanations, mockups, or static pages.

The final result must be a real, production-ready, scalable, SEO-first Persian freelance marketplace.

---

# 1. PRODUCT NAME

The permanent product name is:

# DevJoo

Always write the brand exactly as:

```text
DevJoo
```

Do not rename the product later.

Do not invent temporary names such as:

```text
FreelanceHub
TechMarket
DevMarket
FreelancerApp
```

DevJoo is final.

For technical identifiers only, use:

```text
devjoo
@devjoo/*
```

Potential production domain:

```text
devjoo.ir
```

Never hardcode the production domain.

Use environment variables:

```env
NEXT_PUBLIC_SITE_URL=https://devjoo.ir
API_URL=https://api.devjoo.ir
```

All canonical URLs, structured data, OpenGraph URLs, sitemap URLs and API URLs must be generated from configuration.

---

# 2. BRAND MEANING

DevJoo represents:

```text
Dev + Joo
```

`Dev` represents:

* developers
* technology
* software
* digital professionals

`Joo` is inspired by the Persian concept:

```text
جو / جوینده
```

meaning:

> seeker

The brand concept is:

> A place where professionals find the right projects and employers find the right talent.

DevJoo must NOT be restricted only to programmers.

Development is the primary identity, but the marketplace also supports adjacent technology and digital skills.

---

# 3. PRODUCT POSITIONING

Primary positioning:

> The smart Persian marketplace for technology and digital projects.

Persian positioning:

```text
بازار هوشمند پروژه‌های تکنولوژی و دیجیتال
```

Possible main slogan:

```text
پروژه مناسب، بدون رقابت بیهوده
```

Alternative:

```text
پروژه‌ای که مناسب توئه، پیدات می‌کنه.
```

Employer message:

```text
متخصص مناسب پروژه‌ات را سریع‌تر پیدا کن.
```

DevJoo must NOT look or behave like a generic marketplace containing unrelated categories such as typing, accounting, architecture, etc.

Technology and digital work are the core identity.

---

# 4. ABSOLUTE PRIORITIES

The priorities of the project, in order, are:

1. SEO
2. Product functionality
3. User acquisition
4. Fast onboarding
5. User experience
6. Performance
7. Trust and safety
8. Maintainable architecture
9. Scalability
10. Monetization

SEO is not an afterthought.

SEO must influence:

* architecture
* routing
* database design
* URL design
* content structure
* internal linking
* rendering strategy
* project pages
* category pages
* skill pages
* freelancer pages
* employer pages
* blog
* sitemap
* metadata
* structured data

from DAY ONE.

---

# 5. CRITICAL CONTINUITY SYSTEM

This project will be developed across many sessions.

You MUST NOT restart from scratch every time.

Immediately create these files in the repository root:

```text
AGENTS.md
PROJECT_STATE.md
TODO.md
SEO_PLAN.md
ARCHITECTURE.md
DATABASE.md
DESIGN_SYSTEM.md
API_STATUS.md
CHANGELOG.md
DECISIONS.md
```

These files are mandatory.

---

# 6. AGENTS.md

`AGENTS.md` is the permanent instruction file.

It must summarize and preserve:

* product name
* product vision
* architecture
* frontend stack
* backend stack
* database rules
* API rules
* SEO rules
* RTL rules
* Persian language rules
* design system
* authentication architecture
* security requirements
* testing requirements
* naming conventions
* project directory conventions
* Definition of Done
* rules against fake/mock implementation
* session continuation protocol

Before starting any future task:

READ `AGENTS.md`.

Do not ignore it.

---

# 7. PROJECT_STATE.md

This is the project memory.

Keep it accurate.

Use this exact structure:

```md
# Project
DevJoo

# Current Phase

# Last Completed Task

# Currently Working On

# Completed Features

# Partially Completed Features

# Pending Features

# Important Architecture Decisions

# Database Status

# Migration Status

# API Status

# Frontend Status

# Authentication Status

# SEO Status

# Search Status

# Matching Engine Status

# Notification Status

# Admin Panel Status

# Testing Status

# Production Readiness

# Known Bugs

# Technical Debt

# Blockers

# Last Successful Commands

- pnpm lint
- pnpm typecheck
- pnpm test
- pnpm build

# Recently Modified Files

# Next Recommended Task
```

Update it after every meaningful task.

---

# 8. TODO.md

Create a permanent project checklist.

Every major feature and every important sub-task must be represented by a checkbox.

Example:

```md
## Authentication

- [ ] Authentication database models
- [ ] Google OAuth backend
- [ ] Google OAuth frontend
- [ ] GitHub OAuth backend
- [ ] GitHub OAuth frontend
- [ ] Mobile OTP backend
- [ ] OTP rate limiting
- [ ] Session management
- [ ] Role selection
- [ ] Progressive onboarding
- [ ] Authentication tests
```

A checkbox may only become:

```text
[x]
```

when the functionality is actually implemented.

A static UI is NOT an implemented feature.

---

# 9. DEFINITION OF DONE

A feature is complete only if all relevant items are finished:

* database schema exists
* migrations exist
* backend logic exists
* API exists
* authorization exists
* validation exists
* frontend exists
* frontend uses the real API
* loading state exists
* empty state exists
* error state exists
* responsive design exists
* RTL is correct
* security concerns are addressed
* tests exist where appropriate
* lint passes
* typecheck passes
* build passes

Never mark an unfinished feature as completed.

---

# 10. SESSION CONTINUATION PROTOCOL

At the beginning of every future session:

1. Read `AGENTS.md`
2. Read `PROJECT_STATE.md`
3. Read `TODO.md`
4. Identify the first relevant unfinished task
5. Read only files relevant to that task
6. Continue implementation

Do NOT re-read the entire repository every time.

Do NOT recreate the project.

Do NOT recreate already completed pages.

Do NOT rewrite stable working modules simply because another approach is possible.

Never restart Bootstrap/Foundation unless the project files do not exist.

---

# 11. END-OF-TASK PROTOCOL

After every major implementation task:

1. update `TODO.md`
2. update `PROJECT_STATE.md`
3. update relevant documentation
4. run appropriate tests
5. run typecheck
6. run lint
7. run build when relevant

Record failures truthfully.

Do not hide errors.

Your final response after each coding task should be concise and contain:

```text
Completed:
Files changed:
Tests:
Remaining:
Next task:
```

Do NOT repeat the entire master prompt every session.

---

# 12. PROJECT ARCHITECTURE

Use a TypeScript Monorepo.

Recommended structure:

```text
devjoo/
│
├── apps/
│   ├── web/
│   └── api/
│
├── packages/
│   ├── ui/
│   ├── types/
│   ├── config/
│   ├── api-client/
│   ├── eslint-config/
│   └── typescript-config/
│
├── docker/
├── docs/
├── AGENTS.md
├── PROJECT_STATE.md
├── TODO.md
├── SEO_PLAN.md
├── ARCHITECTURE.md
├── DATABASE.md
├── DESIGN_SYSTEM.md
└── package.json
```

Use:

```text
pnpm
Turborepo
TypeScript strict mode
```

Do not use `any` unless absolutely necessary and documented.

---

# 13. FRONTEND STACK

Use:

```text
Next.js — latest stable
React — stable compatible version
App Router
TypeScript
Tailwind CSS
shadcn/ui where appropriate
Radix UI primitives where appropriate
TanStack Query
React Hook Form
Zod
Lucide Icons
```

Use Server Components by default.

Use Client Components only when real client interaction is necessary.

SEO-critical content must be rendered on the server.

Do NOT create SEO pages where the important content only appears after client-side JavaScript fetches data.

Use:

* SSR
* SSG
* ISR

appropriately.

---

# 14. BACKEND STACK

Use:

```text
NestJS
TypeScript
REST API
OpenAPI / Swagger
```

Backend architecture:

# Modular Monolith

Do NOT start with microservices.

Create clean domain modules so services can be extracted later if necessary.

Recommended modules:

```text
AuthModule
UsersModule
ProfilesModule
FreelancersModule
EmployersModule
SkillsModule
CategoriesModule
ProjectsModule
ProposalsModule
MatchingModule
SearchModule
SavedModule
InvitationsModule
MessagingModule
NotificationsModule
ReviewsModule
VerificationModule
GithubModule
PortfolioModule
PricingModule
AiModule
ServicesModule
TeamsModule
ContractsModule
MilestonesModule
PaymentsModule
AnalyticsModule
ReportsModule
SeoModule
ContentModule
AdminModule
AuditModule
```

---

# 15. DATABASE

Use:

```text
PostgreSQL
Prisma ORM
```

Use the latest stable compatible versions.

Use actual migrations.

Never store important business data in JSON files.

Prefer UUID identifiers.

Use timestamps:

```text
createdAt
updatedAt
deletedAt where required
```

Use soft deletes only where they make business sense.

---

# 16. MONEY

Store money as integer values.

Do NOT use floating point numbers for financial amounts.

Store canonical monetary values in:

```text
IRR / Rial
```

For example:

```text
budgetMinRial
budgetMaxRial
amountRial
```

The frontend may display تومان when appropriate.

Create centralized currency formatting utilities.

---

# 17. DATABASE ENTITIES

Design proper relational entities for at least:

```text
User
Role
UserRole
Session
OAuthAccount
OtpRequest

Profile
FreelancerProfile
EmployerProfile

Category
Skill
UserSkill
SkillVerification

Project
ProjectSkill
ProjectCategory
ProjectStatusEvent
SavedProject

Proposal
ProposalStatusEvent

ProjectInvitation

MatchScore
MatchBreakdown

PortfolioItem
GithubConnection
GithubRepository

EmployerVerification
FreelancerVerification

Review
ReputationScore

Conversation
ConversationMember
Message

Notification
NotificationPreference

ServiceListing
ServiceOrder

Team
TeamMember

Contract
Milestone

Payment
PaymentTransaction

ProjectView
ProfileView
ProposalAnalytics

Report
ModerationAction
AuditLog

BlogPost
BlogCategory
SeoLandingPage
Redirect
```

Add indexes based on real query patterns.

---

# 18. REDIS & BACKGROUND JOBS

Use:

```text
Redis
BullMQ
```

Use Redis for:

* cache
* background tasks
* throttling support
* distributed rate-limit state
* notification jobs
* email jobs
* SMS jobs
* project expiry
* matching calculations
* analytics aggregation
* AI processing

Never perform slow external operations directly inside request-response paths when background processing is appropriate.

---

# 19. OBJECT STORAGE

Create a storage abstraction.

Production must support S3-compatible storage.

Use it for:

* avatars
* portfolio images
* attachments
* company logos
* verification files

Do not store uploaded binary files directly in PostgreSQL.

For local development use an S3-compatible local solution such as MinIO if useful.

---

# 20. PRIMARY ROLES

Users can have:

```text
FREELANCER
EMPLOYER
ADMIN
MODERATOR
```

A normal user can simultaneously be:

```text
FREELANCER + EMPLOYER
```

Do not force users to create separate accounts.

---

# 21. INSTANT JOIN — CRITICAL FEATURE

Registration must feel extremely easy.

The user must NOT see a giant registration form.

Feature name:

# Instant Join

Support:

```text
Continue with Google
Continue with GitHub
Mobile OTP
```

The ideal flow:

```text
Login
↓
Choose role
↓
Enter application
```

Target onboarding time:

```text
under 30 seconds
```

---

# 22. MOBILE OTP

Flow:

```text
Phone number
↓
Send OTP
↓
Enter code
↓
Authenticated
```

Implement:

* OTP expiry
* resend timer
* request rate limiting
* brute-force protection
* maximum verification attempts
* securely hashed OTP codes
* phone number normalization
* audit logging

Do not store OTP values as plaintext.

Create an SMS provider abstraction.

Never hardcode a specific Iranian SMS provider deeply into application logic.

---

# 23. GOOGLE & GITHUB LOGIN

Implement secure OAuth.

Use:

* state protection
* callback validation
* secure sessions
* account linking
* duplicate-account prevention

If the same verified email already exists, offer safe account linking logic rather than silently creating duplicate accounts.

---

# 24. SESSION SECURITY

Do NOT store long-lived authentication tokens in `localStorage`.

Prefer secure session architecture using:

```text
HttpOnly
Secure
SameSite
```

cookies.

Implement:

* session rotation
* session revocation
* logout
* logout all devices
* active sessions page if reasonable

---

# 25. PROGRESSIVE ONBOARDING

Do not force full profile creation before allowing users to explore DevJoo.

Initial question:

```text
برای چه کاری اینجایی؟
```

Options:

```text
دنبال پروژه‌ام
دنبال متخصصم
```

Allow both roles later.

For freelancers, profile completion should happen progressively.

Example:

Before first proposal:

```text
برای ارسال پیشنهاد، ۳ مهارت اصلی خود را انتخاب کنید.
```

Then later suggest:

```text
نمونه‌کار اضافه کنید
GitHub خود را متصل کنید
تجربه کاری را کامل کنید
```

Show profile completion percentage.

Do not block the user unless the missing information is required for that specific action.

---

# 26. PUBLIC ACCESS

Users must NOT be required to log in simply to view marketplace content.

Anonymous users can:

* browse projects
* search projects
* filter projects
* view project detail
* view budgets
* view public freelancer profiles
* view employer profiles where appropriate
* view skills
* view categories
* read blog content
* view service listings

Require authentication only for actions such as:

* send proposal
* save
* follow
* send message
* create project
* invite freelancer
* create service
* purchase service
* review

---

# 27. MAIN MARKETPLACE CATEGORIES

## Development

```text
Frontend
Backend
Full Stack
React
Next.js
Vue
Angular
JavaScript
TypeScript
Node.js
Python
Django
FastAPI
PHP
Laravel
Java
.NET
WordPress
WooCommerce
Mobile Development
Flutter
React Native
Android
iOS
```

## UI/UX & Design

```text
UI Design
UX Design
UI/UX
Figma
Web Design
Product Design
Graphic Design
Design System
```

## AI & Data

```text
Artificial Intelligence
Machine Learning
AI Automation
Chatbots
Data Analysis
Data Engineering
Data Science
```

## DevOps & Infrastructure

```text
Linux
Docker
Kubernetes
CI/CD
Cloud
Server Administration
Infrastructure
```

## QA

```text
QA
Manual Testing
Automation Testing
Performance Testing
```

## Digital

```text
SEO
Technical SEO
Content SEO
Digital Marketing
Performance Marketing
Analytics
```

Development should receive the most prominence.

---

# 28. PROJECT CREATION

Employers must be able to create a project.

Project fields may include:

```text
title
description
category
skills
budget type
minimum budget
maximum budget
fixed/hourly
estimated duration
experience level
remote/on-site/hybrid
city if relevant
deadline
attachments
proposal limit
visibility
```

Project creation must support:

```text
Draft
Preview
Publish
```

Validate everything.

---

# 29. PROJECT STATUS

Use a real project state machine.

Possible statuses:

```text
DRAFT
PENDING_REVIEW
PUBLISHED
PAUSED
IN_PROGRESS
COMPLETED
CANCELLED
EXPIRED
REJECTED
```

Prevent invalid status transitions.

Track status history.

---

# 30. PROPOSALS

Freelancers can send a proposal containing:

```text
price
estimated duration
cover message
milestone proposal if appropriate
optional attachments
```

Proposal statuses:

```text
SUBMITTED
VIEWED
SHORTLISTED
REJECTED
ACCEPTED
WITHDRAWN
```

Track status history.

---

# 31. MAX 10 QUALIFIED PROPOSALS

One of DevJoo's core differentiators.

Default project proposal limit:

```text
10 qualified proposals
```

Example:

```text
6 / 10 پیشنهاد
```

Do NOT automatically allow hundreds of low-quality proposals.

A proposal should only consume a qualified slot if it passes platform rules.

Admin can configure:

```text
default proposal limit
minimum proposal limit
maximum proposal limit
```

Employers may optionally choose other allowed limits.

---

# 32. NO TOKEN BIDDING

Do NOT create a system where freelancers must buy tokens simply to send ordinary proposals.

DevJoo's core positioning includes:

```text
بدون خرید توکن برای ارسال پیشنهاد
```

Combat spam using:

* qualification rules
* rate limits
* reputation
* project relevance
* verification
* proposal limits
* anti-spam systems

not artificial proposal tokens.

---

# 33. DEVJOO MATCH

Implement a real Match Score.

Example:

```text
92% Match
```

This must NOT be random.

Create a dedicated matching engine.

Initial score components can include:

```text
Required skills match          40%
Category relevance             15%
Experience level               10%
Budget compatibility           10%
Availability                   10%
Verified skills                10%
Reputation                     5%
```

Keep weights configurable.

Store useful calculated results.

Show an understandable breakdown:

```text
React             ✓
Next.js           ✓
TypeScript        ✓
Experience        ✓
Budget            ✓
Availability      ✕
```

Do not expose sensitive algorithm internals that enable abuse.

---

# 34. SMART FEED

Each freelancer should receive a personalized project feed.

Default ordering should not simply be:

```text
newest project first
```

Consider:

* Match Score
* freshness
* budget
* preferred skills
* project quality
* employer quality
* user behavior
* saved skills

Tabs can include:

```text
برای شما
جدیدترین
بالاترین تطابق
بودجه بالا
فوری
```

---

# 35. DEVJOO VERIFIED

Implement verification layers.

Possible badges:

```text
GitHub Connected
Email Verified
Phone Verified
Identity Verified
Employer Verified
React Verified
Next.js Verified
WordPress Verified
```

Never display a verification badge unless the verification actually occurred.

---

# 36. GITHUB INTEGRATION

Allow users to connect GitHub.

With authorization and user consent, retrieve relevant public profile/repository information.

Possible use cases:

* GitHub connection badge
* repository portfolio
* technology signals
* activity signals
* profile enrichment

Never falsely claim that GitHub activity automatically proves seniority.

Do not automatically expose private repositories.

---

# 37. GITHUB PORTFOLIO

Allow freelancers to selectively add repositories to their portfolio.

Show appropriate information such as:

* repository name
* description
* languages
* stars if useful
* public URL
* last activity

Users decide which repositories appear.

---

# 38. SKILL BADGES

Create skill verification architecture.

Examples:

```text
React Verified
Next.js Verified
TypeScript Verified
WordPress Verified
```

Verification sources may eventually include:

* GitHub evidence
* portfolio evidence
* tests
* completed DevJoo projects
* manual verification

Keep source information internally.

---

# 39. DEVELOPER LEVEL

Support:

```text
Junior
Mid-Level
Senior
Expert
```

Do not blindly trust self-reported level.

Store:

```text
selfDeclaredLevel
calculatedLevel
verifiedLevel
```

separately where appropriate.

If insufficient evidence exists, do not present algorithmic level as fact.

---

# 40. DEVELOPER REPUTATION

Create a reputation score based on genuine platform behavior.

Potential signals:

* completed projects
* ratings
* response rate
* deadline reliability
* dispute history
* profile verification
* employer feedback
* repeat clients

Prevent review manipulation.

---

# 41. CLIENT SCORE

Freelancers must be able to evaluate employers before spending time on proposals.

Display useful employer metrics such as:

```text
Hire Rate
Response Rate
Projects Posted
Projects Hired
Completed Projects
Average Response Time
Verified Status
Member Since
```

Example:

```text
Hire Rate: 78%
Response Rate: 93%
9 hires from 12 projects
```

Never fabricate values.

Hide metrics when insufficient data exists.

---

# 42. EMPLOYER VERIFICATION

Support employer verification.

Possible levels:

```text
Phone Verified
Email Verified
Identity Verified
Company Verified
Payment Verified
```

Verification details must be stored securely.

Do not expose private documents.

---

# 43. LIVE PROJECT STATUS

Freelancers should understand what is happening after submitting a proposal.

Possible visible states:

```text
پیشنهاد ارسال شد
کارفرما پیشنهاد را مشاهده کرد
به لیست منتخب اضافه شدید
فریلنسر انتخاب شد
پروژه بسته شد
```

Do not expose private information about other freelancers.

---

# 44. GHOST PROTECTION

Projects must not remain active forever.

Implement inactivity detection.

Example rules:

If an employer:

* never views proposals
* does not log in
* does not interact with the project

for a configurable period, mark the project appropriately.

Possible state:

```text
Inactive
```

Eventually:

```text
Expired
```

Notify the employer before expiration.

This prevents the marketplace from feeling dead.

---

# 45. PROJECT QUALITY SCORE

Calculate an internal Project Quality Score.

Potential signals:

* detailed description
* realistic budget
* verified employer
* complete skills
* reasonable deadline
* employer history
* duplicate content
* suspicious behavior

May display a simplified indicator:

```text
کیفیت آگهی: بالا
```

Do not expose anti-fraud internals.

---

# 46. DUPLICATE PROJECT DETECTION

Detect duplicate or near-duplicate project postings.

Start using:

* normalized title similarity
* description similarity
* employer history
* skill overlap
* recent posting similarity

The architecture may later use embeddings or AI.

Flag suspicious duplicates for moderation.

Do not automatically delete without appropriate confidence.

---

# 47. REVERSE HIRING

Employers should not always need to wait for proposals.

After posting a project:

DevJoo should identify suitable freelancers.

Example:

```text
5 متخصص مناسب پروژه شما
```

Employer can:

```text
Invite to Project
```

Freelancers receive:

```text
این پروژه 94٪ با پروفایل شما تطابق دارد.
```

---

# 48. PROJECT INVITATIONS

Employers can invite freelancers directly.

Track:

```text
SENT
VIEWED
ACCEPTED
DECLINED
EXPIRED
```

Prevent invitation spam.

---

# 49. AVAILABILITY

Freelancers can select:

```text
Available
Limited Availability
Busy
Unavailable
```

Optionally allow:

```text
availableFrom
hoursPerWeek
```

Match Score can use this data.

---

# 50. AI PROJECT BUILDER

Create an AI-assisted project creation flow.

Example employer input:

```text
I need an admin dashboard like this website.
```

AI can help generate:

* project title
* structured description
* scope
* suggested skills
* estimated complexity
* deliverables
* missing questions

The employer MUST review and approve before publication.

AI must never automatically publish a project.

Create an `AIProvider` abstraction.

Do not hardcode one AI company deeply into business logic.

Environment variables hold API credentials.

---

# 51. AI PROPOSAL ASSISTANT

Allow freelancers to improve proposal wording.

AI may help with:

* clarity
* professionalism
* structure
* grammar
* relevance

AI must not:

* invent experience
* invent portfolio projects
* invent credentials
* automatically send proposals

The freelancer approves the final content.

---

# 52. PRICE RADAR

Create:

# Price Radar

Purpose:

Help employers and freelancers understand realistic project pricing.

Initially use real DevJoo historical data.

Consider:

* category
* skills
* complexity
* project duration
* experience level
* similar completed projects

Example:

```text
بودجه این پروژه نسبت به پروژه‌های مشابه پایین است.
```

or:

```text
پیشنهاد شما نزدیک به میانگین پروژه‌های مشابه است.
```

CRITICAL:

If there is insufficient data:

DO NOT fabricate market prices.

Show:

```text
داده کافی برای تخمین دقیق قیمت وجود ندارد.
```

---

# 53. HIRING PROBABILITY

Create an optional hiring likelihood indicator based on genuine employer history.

Possible signals:

* employer hire rate
* project completeness
* verified status
* recent activity
* proposal viewing behavior

Example:

```text
احتمال استخدام: بالا
```

Never claim certainty.

If there is insufficient data, hide the metric.

---

# 54. SIMILAR PROJECTS

Every public project page should display related projects.

Use:

* category
* skills
* semantic similarity
* budget
* project type

Benefits:

* UX
* user retention
* internal linking
* SEO crawl depth

---

# 55. STRUCTURED REJECTION

When rejecting a proposal, employers may optionally choose a reason:

```text
بودجه نامناسب
تجربه کافی نیست
مهارت‌های موردنیاز کامل نیست
فریلنسر دیگری انتخاب شد
زمان‌بندی مناسب نیست
سایر
```

Do not allow abusive custom rejection content.

Freelancers can use anonymized aggregate rejection feedback to improve.

---

# 56. PROPOSAL ANALYTICS

Freelancers should have useful analytics.

Examples:

```text
Proposal submitted
Proposal viewed
Profile viewed
Shortlisted
Accepted
```

Dashboard statistics:

```text
View Rate
Shortlist Rate
Hire Rate
```

Avoid vanity metrics that do not help users.

---

# 57. PAID TRIAL

For suitable larger projects, employers can create a small paid trial task before the main contract.

The system must make clear:

```text
تست پولی
```

Do not encourage unpaid speculative work.

Paid Trial should later integrate with payment/milestone infrastructure.

---

# 58. MILESTONES

Support contracts divided into milestones.

Each milestone can contain:

```text
title
description
amount
due date
status
```

Statuses:

```text
DRAFT
FUNDED
IN_PROGRESS
SUBMITTED
APPROVED
DISPUTED
PAID
CANCELLED
```

Implement architecture carefully.

---

# 59. PAYMENT ARCHITECTURE

Create a provider-based payment architecture.

Do NOT tightly couple the platform to one payment provider.

Example:

```ts
interface PaymentProvider {}
```

Production payment functionality must be feature-flagged if real credentials or required legal/compliance setup is unavailable.

Never fake successful production payments.

Development mode may use a clearly labeled fake provider.

Potential future functionality:

* escrow-like payment
* milestone payment
* service order payment
* refunds
* commissions
* platform fees

---

# 60. SERVICE MARKETPLACE

Freelancers should also be able to sell predefined services.

Examples:

```text
تبدیل Figma به React
طراحی Landing Page با Next.js
رفع مشکل Performance سایت
طراحی UI Dashboard
بهینه‌سازی Technical SEO
```

Each service can contain:

* title
* description
* category
* skills
* price
* delivery time
* revisions
* requirements
* portfolio
* FAQ

Public service pages must also be SEO-friendly.

---

# 61. TEAM MODE

Allow freelancers to create teams.

Examples:

```text
Frontend + Backend
UI/UX + Frontend
Full product team
```

Team entities:

```text
Team
TeamMember
TeamRole
```

Employers may invite:

```text
individual freelancer
or
team
```

---

# 62. MESSAGING

Build real messaging.

Features:

* conversations
* messages
* unread counters
* timestamps
* attachments if enabled
* read status if appropriate
* moderation/report tools

Use WebSocket or another appropriate real-time approach.

NestJS WebSocket Gateway is acceptable.

Use Redis adapter if necessary for horizontal scaling.

Protect messaging from:

* spam
* abuse
* unauthorized conversation access
* IDOR vulnerabilities

---

# 63. NOTIFICATIONS

Support:

```text
In-app
Email
SMS where appropriate
```

Notification examples:

* new matching project
* new proposal
* proposal viewed
* shortlisted
* project invitation
* message
* project expiring
* proposal accepted
* milestone update
* review reminder

Allow notification preferences.

Use background jobs.

---

# 64. INSTANT PROJECT ALERT

Freelancers may subscribe to selected skills.

Examples:

```text
React
Next.js
WordPress
SEO
UI/UX
```

When a highly relevant project is published:

send an appropriate notification.

Do not spam users.

Use digesting/rate limits where appropriate.

---

# 65. REVIEWS

Only allow reviews after legitimate completed work.

Consider bilateral reviews.

To reduce retaliation bias:

reviews may remain hidden until:

* both sides submit
  or
* review period ends

Possible categories:

Freelancer:

```text
Quality
Communication
Deadline
Professionalism
```

Employer:

```text
Communication
Requirements Clarity
Payment Reliability
Professionalism
```

---

# 66. REPORTING & MODERATION

Users can report:

* project
* user
* message
* proposal
* service
* review

Report reasons:

```text
Spam
Fraud
Abuse
Fake project
Copyright
Payment issue
Other
```

Create an admin moderation queue.

Track moderator actions.

---

# 67. ADMIN PANEL

Build a real admin dashboard.

Admin capabilities:

## Dashboard

* new users
* projects
* proposals
* hires
* active projects
* conversion metrics
* reports

## Users

* search
* view
* suspend
* ban
* verify
* inspect reports

## Projects

* approve
* reject
* edit moderation status
* feature
* expire
* detect spam

## Skills

* create
* edit
* merge duplicates
* manage synonyms

## Categories

* CRUD
* hierarchy
* SEO metadata

## SEO

* manage landing pages
* title templates
* meta descriptions
* index/noindex
* redirects
* canonical rules
* sitemap visibility

## Content

* blog management
* FAQ management

## Configuration

* default proposal limit
* inactivity thresholds
* feature flags
* matching weights

All admin actions must be permission-controlled and audited.

---

# 68. SEARCH — VERY IMPORTANT

Search is a core feature.

Initial search should use PostgreSQL.

Support:

* title
* description
* skills
* category
* freelancer
* employer
* services

Use appropriate:

```text
GIN indexes
pg_trgm
```

where beneficial.

---

# 69. PERSIAN SEARCH NORMALIZATION

Build a reusable Persian normalization layer.

Normalize differences such as:

```text
ي → ی
ك → ک
```

Handle:

* Arabic/Persian characters
* Persian digits
* English digits
* extra spaces
* ZWNJ
* punctuation
* English technology names
* case normalization

Examples:

```text
react
React
REACT
```

should match appropriately.

Likewise:

```text
ری اکت
ری‌اکت
React
```

may be associated through skill synonyms.

---

# 70. SKILL SYNONYMS

Support search aliases.

Example:

```text
React
React.js
ReactJS
ری اکت
ری‌اکت
```

Map them to a canonical skill.

Another:

```text
Next.js
NextJS
Next
نکست
نکست جی اس
```

Use synonym tables rather than hardcoded hacks everywhere.

---

# 71. SEARCH ADAPTER

Do not tightly couple application logic to PostgreSQL search.

Create a search abstraction.

Initial implementation:

```text
PostgreSQL Search Adapter
```

Future adapters may include:

```text
OpenSearch
Elasticsearch
Meilisearch
```

Do not add heavy search infrastructure prematurely.

---

# 72. SEO — HIGHEST PRIORITY

SEO is one of DevJoo's competitive advantages.

Do NOT treat SEO as:

```text
just add title and meta description
```

Build a complete SEO architecture.

---

# 73. TARGET PERSIAN KEYWORDS

DevJoo should naturally target search intents such as:

```text
پروژه فریلنسری
پروژه فریلنسری برنامه نویسی
پروژه برنامه نویسی
گرفتن پروژه برنامه نویسی
پروژه دورکاری
کار فریلنسری
سایت فریلنسری
فریلنسر برنامه نویس
پروژه طراحی سایت
پروژه طراحی وب
پروژه فرانت اند
پروژه frontend
پروژه react
پروژه React
پروژه next js
پروژه Next.js
پروژه جاوا اسکریپت
پروژه TypeScript
پروژه Node.js
پروژه Python
پروژه Laravel
پروژه وردپرس
پروژه UI UX
پروژه طراحی رابط کاربری
پروژه سئو
استخدام فریلنسر
استخدام برنامه نویس
استخدام برنامه نویس فرانت اند
استخدام برنامه نویس React
استخدام برنامه نویس Next.js
استخدام برنامه نویس دورکار
استخدام طراح UI UX
استخدام متخصص سئو
```

IMPORTANT:

Never keyword-stuff pages.

Write natural Persian content satisfying the actual search intent.

---

# 74. SEO KEYWORD CLUSTERS

Create keyword clusters.

Example:

## Frontend Cluster

```text
پروژه فرانت اند
پروژه frontend
پروژه طراحی فرانت اند
استخدام برنامه نویس فرانت اند
فریلنسر فرانت اند
برنامه نویس frontend
```

## React Cluster

```text
پروژه React
پروژه ری اکت
استخدام برنامه نویس React
فریلنسر React
برنامه نویس React دورکار
```

## Next.js Cluster

```text
پروژه Next.js
پروژه NextJS
استخدام برنامه نویس Next.js
فریلنسر Next.js
```

Do the same for major skills.

Record keyword strategy in:

```text
SEO_PLAN.md
```

---

# 75. SEO PAGE ARCHITECTURE

Create intentional indexable landing pages.

Examples:

```text
/
/projects
/projects/frontend
/projects/react
/projects/nextjs
/projects/wordpress
/projects/python
/projects/ui-ux
/projects/seo

/hire
/hire/frontend-developer
/hire/react-developer
/hire/nextjs-developer
/hire/wordpress-developer
/hire/ui-ux-designer
/hire/seo-specialist

/freelancers
/freelancers/[username]

/project/[slug]-[shortId]

/services
/services/[slug]-[shortId]

/blog
/blog/[slug]
```

Do NOT automatically index every possible filter combination.

---

# 76. FACETED SEO RULE

Filters such as:

```text
?budget=
?sort=
?experience=
?remote=
?page=
```

must NOT create unlimited indexable URL combinations.

Avoid index bloat.

Most search/filter parameter combinations should:

```text
noindex,follow
```

or canonicalize appropriately.

Only create indexable landing pages for intentional keyword targets.

---

# 77. SEO URL RULES

URLs should be:

* stable
* short
* descriptive
* lowercase
* predictable

Examples:

```text
/projects/react
/projects/frontend
/hire/react-developer
/project/build-saas-dashboard-82fa91
```

Avoid:

```text
/project?id=583829
```

as the primary public URL.

Use IDs internally while preserving readable slugs.

Do not break old URLs when titles change.

---

# 78. DYNAMIC METADATA

Every important public page must generate appropriate:

```text
<title>
meta description
canonical
OpenGraph
Twitter metadata
robots
```

Titles should target user intent first and DevJoo brand second.

BAD:

```text
DevJoo | React
```

GOOD:

```text
پروژه React و استخدام برنامه نویس React | DevJoo
```

---

# 79. HOMEPAGE SEO

Suggested title:

```text
پروژه فریلنسری، برنامه نویسی و دیجیتال | DevJoo
```

Suggested meta description:

```text
در DevJoo پروژه‌های برنامه نویسی، فرانت اند، React، Next.js، وردپرس، طراحی UI/UX، سئو و سایر حوزه‌های دیجیتال را پیدا کنید یا متخصص مناسب پروژه خود را استخدام کنید.
```

Suggested H1:

```text
پروژه مناسب مهارتت را پیدا کن
```

Do NOT make H1 a giant comma-separated keyword list.

---

# 80. CATEGORY SEO PAGES

Example:

```text
/projects/frontend
```

Title:

```text
پروژه فرانت اند و استخدام برنامه نویس Frontend | DevJoo
```

H1:

```text
پروژه‌های فرانت اند
```

Page can contain:

* active Frontend projects
* short useful introduction
* common Frontend skills
* related skill pages
* FAQ
* related projects
* information for employers
* internal links

Do not generate meaningless AI filler.

---

# 81. SKILL SEO PAGES

Example:

```text
/projects/react
```

Possible title:

```text
پروژه React و استخدام فریلنسر React | DevJoo
```

Useful content:

* active projects
* related skills
* market information based on real data
* FAQ
* popular project types
* employer CTA
* freelancer CTA

Repeat for strategic skills.

---

# 82. HIRE LANDING PAGES

Create employer-intent pages.

Example:

```text
/hire/react-developer
```

Possible title:

```text
استخدام برنامه نویس React فریلنسر و دورکار | DevJoo
```

Content:

* verified React freelancers
* how hiring works
* relevant skills
* project examples
* FAQ
* CTA to post project

Do not duplicate content from `/projects/react`.

Search intent must differ.

---

# 83. PROJECT DETAIL SEO

Every public project should have a unique permanent page.

Example:

```text
/project/nextjs-admin-dashboard-4fa829
```

Include:

* project title
* description
* budget
* skills
* category
* publish date
* project state
* employer trust indicators
* relevant projects
* breadcrumbs

Do not expose confidential details.

---

# 84. CLOSED PROJECT SEO

Do not automatically delete valuable project URLs when a project is completed.

When appropriate:

Keep the public page accessible and clearly show:

```text
این پروژه بسته شده است.
```

Then provide:

```text
مشاهده پروژه‌های مشابه
```

This preserves useful long-tail landing pages and backlinks.

However:

If a page is:

* spam
* illegal
* duplicate
* extremely thin
* intentionally removed

use appropriate:

```text
404
410
noindex
```

Do not keep low-quality pages indexed merely for SEO quantity.

---

# 85. INDEX QUALITY RULE

SEO goal is NOT:

> index as many pages as possible

SEO goal is:

> index useful pages that satisfy a real user search.

Prevent thin-content index pollution.

---

# 86. CANONICALS

Every indexable page needs correct canonical behavior.

Canonical URLs must:

* use production site configuration
* exclude tracking parameters
* avoid duplicate filtered versions
* stay stable

Never canonicalize unrelated pages to the homepage.

---

# 87. SITEMAP

Create dynamic sitemap architecture.

Eventually support sitemap index and separate sitemaps such as:

```text
sitemap-projects.xml
sitemap-skills.xml
sitemap-categories.xml
sitemap-freelancers.xml
sitemap-services.xml
sitemap-blog.xml
```

Only include canonical indexable URLs.

Do not include:

* login
* register
* dashboard
* admin
* settings
* private messages
* internal search pages
* noindex URLs

`lastmod` must reflect real content changes.

Do NOT set every URL's last modification time to "now" on every sitemap generation.

---

# 88. ROBOTS.TXT

Generate a production-aware `robots.txt`.

Block crawling of irrelevant/private areas such as:

```text
/admin
/dashboard
/settings
/messages
/api
```

Do not accidentally block public project pages, category pages, or CSS/JS assets needed for rendering.

Reference the sitemap.

---

# 89. STRUCTURED DATA

Implement valid JSON-LD where appropriate.

Potential schemas:

```text
Organization
WebSite
BreadcrumbList
ProfilePage
Person
Article
FAQPage when actually eligible and useful
Service
ItemList
```

Use `JobPosting` ONLY when a listing genuinely satisfies applicable job-posting requirements.

Do NOT misuse structured data just to attempt rich results.

Do NOT create fake:

```text
AggregateRating
Review
Salary
```

data.

Structured data must match visible page content.

---

# 90. BREADCRUMBS

Public SEO pages must use breadcrumbs.

Example:

```text
DevJoo
>
پروژه‌ها
>
برنامه نویسی
>
React
```

Render visible breadcrumbs and corresponding structured data where appropriate.

---

# 91. INTERNAL LINKING

Build strong contextual internal linking.

Examples:

React project page links to:

```text
React projects
Frontend projects
Next.js projects
React freelancers
Related projects
```

Skill pages link to:

* related skills
* freelancer pages
* projects
* hiring pages
* useful articles

Do not create giant spammy footer link farms.

---

# 92. BLOG / CONTENT HUB

Create a real blog architecture.

Possible SEO topics:

```text
چگونه پروژه برنامه نویسی بگیریم؟
چگونه فریلنسر React استخدام کنیم؟
قیمت پروژه React چقدر است؟
تفاوت React و Next.js برای پروژه
چگونه پروژه فرانت اند قیمت گذاری کنیم؟
چگونه یک توضیح پروژه خوب بنویسیم؟
```

Blog pages:

```text
/blog
/blog/[slug]
```

Admin can create/edit articles.

Support:

* title
* slug
* excerpt
* body
* cover
* author
* category
* publish date
* update date
* SEO metadata

Do not mass-generate low-quality AI articles.

---

# 93. SEO CONTENT QUALITY

Never create doorway pages.

Never create hundreds of nearly identical pages where only one keyword changes.

Every indexable landing page must provide unique practical value.

No keyword stuffing.

No hidden SEO text.

No cloaking.

No fake reviews.

No fake project counts.

No fake salaries.

No fake statistics.

---

# 94. SEARCH ENGINE PERFORMANCE

Optimize Core Web Vitals.

Targets:

```text
LCP < 2.5s
INP < 200ms
CLS < 0.1
```

Treat these as performance targets, not fake guarantees.

Use:

* Server Components
* image optimization
* code splitting
* lazy loading
* caching
* optimized fonts
* minimal JavaScript
* appropriate CDN caching

---

# 95. PERSIAN LANGUAGE

Primary language:

```text
fa-IR
```

Root HTML:

```html
<html lang="fa" dir="rtl">
```

All primary UI copy must be professionally written Persian.

Avoid machine-like Persian.

Avoid mixing Persian and English unnecessarily.

Technical terms such as:

```text
React
Next.js
Node.js
GitHub
TypeScript
UI/UX
```

can remain English when natural.

---

# 96. PERSIAN TYPOGRAPHY

Use:

# Vazirmatn

Self-host the font.

Use only required weights, such as:

```text
400
500
600
700
800
```

Optimize font loading.

Avoid downloading fonts from external third-party CDNs at runtime.

---

# 97. RTL QUALITY

RTL must be implemented properly.

Do not simply add:

```css
direction: rtl;
```

and assume everything works.

Audit:

* navigation
* icons
* arrows
* forms
* dropdowns
* selects
* pagination
* tables
* breadcrumbs
* sidebars
* modals
* toasts
* numbers
* mixed Persian/English
* mobile navigation

Technology names should remain readable in LTR where necessary.

---

# 98. VISUAL IDENTITY

DevJoo should feel:

```text
Modern
Professional
Technical
Smart
Premium
Trustworthy
Minimal
Fast
```

Do NOT design it like:

```text
Cyberpunk
Gaming
Hacker
Crypto scam
Neon dashboard
```

---

# 99. PURPLE DESIGN SYSTEM

Primary:

```css
--primary: #7C3AED;
```

Secondary purple:

```css
--primary-light: #8B5CF6;
```

Dark purple:

```css
--primary-dark: #6D28D9;
```

Soft purple:

```css
--primary-soft: #F5F3FF;
```

Base colors:

```css
--background: #F8FAFC;
--surface: #FFFFFF;
--text-primary: #0F172A;
--text-secondary: #475569;
--border: #E2E8F0;
--success: #16A34A;
--warning: #D97706;
--danger: #DC2626;
```

Purple should primarily be used for:

* main CTA
* brand accents
* selected items
* active navigation
* Match Score
* important badges
* key highlights

Do not turn the entire site purple.

---

# 100. GRADIENT

Use gradients sparingly.

Approved brand gradient:

```text
#6D28D9 → #8B5CF6
```

Do not place gradients behind every component.

---

# 101. DARK MODE

Implement Dark Mode.

Dark Mode must be designed, not automatically inverted.

Remember Persian text readability and appropriate contrast.

User preference can be stored.

---

# 102. COMPONENT STYLE

Cards should generally use:

```text
12px–16px radius
subtle border
very subtle shadow
clean spacing
```

Avoid:

* excessive glassmorphism
* excessive blur
* huge shadows
* excessive animations
* overly rounded childish UI

---

# 103. HOMEPAGE

Homepage should be clean and conversion-oriented.

Suggested structure:

## Header

Logo:

```text
DevJoo
```

Navigation:

```text
پروژه‌ها
فریلنسرها
دسته‌بندی‌ها
خدمات
وبلاگ
```

Actions:

```text
ورود
ثبت پروژه
```

---

# 104. HOMEPAGE HERO

Suggested H1:

```text
پروژه مناسب مهارتت را پیدا کن
```

Suggested supporting copy:

```text
پروژه‌های واقعی و مرتبط با مهارتت را ببین، بدون رقابت بین صدها پیشنهاد بی‌ربط.
```

CTAs:

```text
پروژه پیدا کن
متخصص پیدا کن
```

Include a prominent project search.

Do not force login.

---

# 105. HOMEPAGE ACTIVE PROJECTS

Display real active projects.

Each card can show:

```text
Project title
Skills
Budget
Publish time
Match Score if logged in
Proposal count
Employer verification
Project quality
```

Example:

```text
طراحی داشبورد SaaS با Next.js

React
Next.js
TypeScript

92% تطابق

بودجه: ۳۰ تا ۴۵ میلیون تومان

پیشنهادها: 6 / 10

کارفرمای تأییدشده
```

---

# 106. HOMEPAGE SKILLS

Show popular skills:

```text
React
Next.js
WordPress
Python
UI/UX
SEO
Node.js
AI
```

Each links to an SEO landing page.

---

# 107. DIFFERENTIATORS SECTION

Show clearly:

```text
DevJoo Match
حداکثر پیشنهادهای مرتبط
بدون خرید توکن
کارفرمای قابل ارزیابی
متخصص‌های تأییدشده
ثبت‌نام سریع
```

---

# 108. PROJECT LIST PAGE

Build:

```text
/projects
```

Filters:

* search
* category
* skills
* budget
* experience level
* remote
* project type
* employer verification
* proposal availability

Sort:

```text
پیشنهادی برای شما
جدیدترین
بیشترین تطابق
بیشترین بودجه
```

Filters must work through real backend query parameters.

---

# 109. PROJECT CARD

Reusable project card should support:

* title
* summary
* skills
* budget
* date
* employer
* verified badge
* Match Score
* proposal slots
* save button
* project quality
* urgency

Keep cards easy to scan.

---

# 110. FREELANCER PROFILE

Public freelancer page can include:

```text
name
headline
avatar
location
availability
bio
skills
verified badges
experience
portfolio
GitHub
completed DevJoo projects
ratings
reputation
response rate
services
```

Do not display private contact information by default.

---

# 111. EMPLOYER PROFILE

Public employer profile may show:

```text
name/company
verification
member since
projects posted
hire rate
response rate
completed projects
reviews
```

Protect sensitive information.

---

# 112. FREELANCER DASHBOARD

Important sections:

```text
پروژه‌های مناسب شما
پیشنهادهای من
دعوت‌ها
پیام‌ها
نمونه‌کارها
خدمات من
آمار
پروفایل
```

Main dashboard heading could be:

```text
۱۲ پروژه مناسب شما پیدا کردیم
```

Prioritize meaningful actions rather than useless charts.

---

# 113. EMPLOYER DASHBOARD

Include:

```text
پروژه‌های من
پیشنهادهای دریافتی
متخصص‌های پیشنهادی
دعوت‌ها
پیام‌ها
قراردادها
پرداخت‌ها
```

---

# 114. RESPONSIVE DESIGN

The entire application must work on:

* mobile
* tablet
* laptop
* desktop

Mobile UX is not optional.

Do not merely shrink desktop layouts.

---

# 115. ACCESSIBILITY

Use semantic HTML.

Implement:

* labels
* keyboard navigation
* focus states
* accessible modal behavior
* sufficient contrast
* meaningful aria attributes where necessary

Do not sacrifice accessibility for design.

---

# 116. API DESIGN

Version APIs:

```text
/api/v1/*
```

Use consistent resource naming.

Examples:

```text
GET    /api/v1/projects
GET    /api/v1/projects/:id
POST   /api/v1/projects
PATCH  /api/v1/projects/:id

POST   /api/v1/projects/:id/proposals

GET    /api/v1/me
PATCH  /api/v1/me/profile
```

Generate OpenAPI documentation.

---

# 117. VALIDATION

Validate data on BOTH:

* frontend
* backend

Backend validation is authoritative.

Never trust browser input.

Use DTO validation appropriately.

---

# 118. API ERRORS

Create a consistent API error format.

Include:

```text
code
message
field errors if appropriate
request id
```

Do not expose:

* stack traces
* SQL errors
* secrets
* internal paths

in production.

---

# 119. PAGINATION

Use cursor pagination where appropriate for large feeds.

Use conventional pagination where SEO requires crawlable page structure.

Do not force the same pagination style everywhere.

---

# 120. SECURITY

Implement security seriously.

Protect against:

* XSS
* CSRF where applicable
* SQL injection
* authorization bypass
* IDOR
* brute force
* OTP abuse
* OAuth attacks
* spam
* mass assignment
* file upload abuse
* rate abuse

Use server-side authorization on every protected resource.

Never rely only on hiding buttons in the frontend.

---

# 121. RATE LIMITS

Different actions need different limits.

Examples:

* OTP requests
* login
* messages
* proposals
* project posting
* AI requests
* search
* reports

Use meaningful per-user/per-IP rules.

---

# 122. AUDIT LOGGING

Track security-sensitive/admin operations.

Examples:

```text
user suspended
verification approved
project removed
role changed
SEO redirect changed
payment manually modified
```

Audit logs should include:

* actor
* action
* resource
* timestamp
* relevant metadata

---

# 123. FILE UPLOAD SECURITY

Validate:

* file type
* MIME
* size
* extension
* user authorization

Use generated storage filenames.

Do not trust original filenames.

Create future-compatible hooks for malware scanning.

---

# 124. LOGGING

Use structured server logs.

Include:

* request id
* severity
* context
* timestamp

Never log:

* passwords
* OTP codes
* tokens
* sensitive verification documents

---

# 125. ANALYTICS

Build first-party product analytics events where useful.

Track meaningful events such as:

```text
project_view
project_search
project_saved
proposal_started
proposal_submitted
project_created
freelancer_invited
signup_completed
github_connected
```

Do not collect unnecessary personal information.

---

# 126. SEO ANALYTICS PREPARATION

Prepare clean integration points for:

```text
Google Search Console
Google Analytics or privacy-compatible analytics
```

Do not hardcode analytics IDs.

Use environment variables.

---

# 127. TESTING

Use a serious test strategy.

Frontend:

```text
unit/component tests where valuable
Playwright for important E2E flows
```

Backend:

```text
unit tests
integration tests
API tests
```

Critical E2E flows:

1. Register with OTP
2. Select freelancer role
3. Browse project
4. Send proposal
5. Employer receives proposal
6. Employer shortlists proposal
7. Freelancer receives notification

and:

1. Employer login
2. Create project
3. Publish project
4. Project appears publicly
5. SEO metadata renders
6. Freelancer match calculation works

---

# 128. SEO TESTING

Add automated checks where reasonable for:

* title exists
* canonical exists
* robots rules
* sitemap generation
* structured data validity structure
* public page status codes
* noindex private pages

---

# 129. DEVELOPMENT ENVIRONMENT

Provide:

```text
docker-compose.yml
.env.example
```

Local infrastructure may include:

```text
PostgreSQL
Redis
MinIO
Mail testing service
```

Developers should be able to run the project with documented commands.

---

# 130. ENVIRONMENT VARIABLES

Create `.env.example`.

Never commit secrets.

Examples:

```env
DATABASE_URL=
REDIS_URL=

NEXT_PUBLIC_SITE_URL=
API_URL=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

SMS_PROVIDER=
SMS_API_KEY=

AI_PROVIDER=
AI_API_KEY=

S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=
```

Validate required environment variables at application startup.

---

# 131. SEED DATA

Create realistic development seed data.

Use Persian sample content.

Seed:

* categories
* skills
* users
* employers
* freelancers
* projects

Never show seed/demo data in production.

---

# 132. CI

Create CI workflow.

Run:

```text
install
lint
typecheck
test
build
```

A pull request should not pass when these fail.

---

# 133. CODE QUALITY

Rules:

* strict TypeScript
* meaningful names
* avoid giant components
* avoid giant services
* separate business logic
* reusable components
* reusable domain services
* no unnecessary abstractions
* no premature microservices
* no copy-paste business logic

---

# 134. ERROR UX

Never leave the user with a blank screen.

Implement:

* skeleton/loading states
* empty states
* retry states
* useful error messages
* 404
* 500 handling

Persian copy must be user-friendly.

---

# 135. FEATURE FLAGS

Implement feature flag architecture for larger or regulatory-dependent functionality.

Examples:

```text
PAYMENTS_ENABLED
PAID_TRIAL_ENABLED
AI_PROJECT_BUILDER_ENABLED
AI_PROPOSAL_ASSISTANT_ENABLED
TEAM_MODE_ENABLED
```

Do not scatter environment-condition checks everywhere.

Create a centralized feature flag service/config.

---

# 136. MONETIZATION

Initial philosophy:

Do NOT monetize normal proposal submission with tokens.

Potential revenue streams:

Employer:

```text
Featured project
Urgent project
Highlighted listing
Premium employer tools
```

Freelancer:

```text
Premium profile
Advanced analytics
Verification
Portfolio enhancements
```

Marketplace:

```text
Service marketplace fees
Future contract/payment fees
```

Do not make free users intentionally unusable.

---

# 137. PROJECT ROADMAP

Follow this order unless an existing repository state requires continuing elsewhere.

## PHASE 0 — Foundation

```text
Repository
Monorepo
Next.js
NestJS
PostgreSQL
Prisma
Redis
Docker
Environment validation
Shared packages
Project memory files
```

## PHASE 1 — Design System + SEO Foundation

```text
RTL
Vazirmatn
Purple design system
Layout
Header
Footer
Metadata architecture
robots
sitemap architecture
canonical helpers
structured data helpers
```

## PHASE 2 — Authentication

```text
User database
Sessions
OTP
Google
GitHub
Role selection
Instant Join
Progressive onboarding
```

## PHASE 3 — Marketplace Core

```text
Categories
Skills
Projects
Project pages
Search
Filters
Save project
Proposal system
Proposal limit
Employer dashboard
Freelancer dashboard
```

## PHASE 4 — SEO Landing Pages

```text
Project categories
Skill pages
Hire pages
Internal links
Breadcrumbs
Blog foundation
```

## PHASE 5 — Trust

```text
Client Score
Employer verification
Freelancer verification
GitHub integration
Portfolio
Reviews
Reputation
```

## PHASE 6 — Smart Features

```text
DevJoo Match
Smart Feed
Reverse Hiring
Project invitations
Availability
Project Quality Score
Duplicate Detection
Hiring Probability
```

## PHASE 7 — Communication

```text
Messaging
Notifications
Instant alerts
Email jobs
SMS jobs
```

## PHASE 8 — AI

```text
AI Provider abstraction
AI Project Builder
AI Proposal Assistant
```

## PHASE 9 — Analytics

```text
Proposal analytics
Project analytics
Employer metrics
Price Radar
```

## PHASE 10 — Advanced Marketplace

```text
Services
Teams
Paid Trial
Milestones
Contracts
Payment abstraction
```

## PHASE 11 — Admin

```text
Admin dashboard
Moderation
Reports
SEO control
Content control
Feature configuration
```

## PHASE 12 — Production Hardening

```text
Security audit
SEO audit
Accessibility audit
Performance
Tests
CI
Logging
Monitoring
Deployment documentation
```

---

# 138. MVP PRIORITY WITHIN THE FULL PRODUCT

Although all listed features belong to the full product, prioritize these features first because they define DevJoo's initial competitive advantage:

```text
1. SEO architecture
2. Instant Join
3. Project marketplace
4. Smart Search
5. DevJoo Match
6. Maximum Qualified Proposals
7. No Token Bidding
8. Client Score
9. DevJoo Verified
10. Smart Feed
11. Live Project Status
12. Ghost Protection
```

Do not spend weeks building payment infrastructure before the marketplace itself works.

---

# 139. DO NOT USE FAKE FEATURES

Forbidden:

```text
random Match Score
fake ratings
fake user counts
fake project counts
fake notifications
fake Price Radar
fake hiring probability
fake employer verification
fake payments
static hardcoded dashboard stats
```

If real data does not exist:

show an honest empty state.

---

# 140. DO NOT USE MOCK BACKEND IN FINAL FEATURES

During early development temporary mocks may be used only if necessary.

But before marking a feature complete:

replace mock data with the real backend/database.

Frontend-only functionality does not count as completed.

---

# 141. DO NOT REBUILD WORKING FEATURES

If a feature already works:

inspect it before modifying it.

Do not rebuild the entire application because you prefer another style.

Make incremental changes.

Protect existing behavior with tests when appropriate.

---

# 142. DO NOT ASK UNNECESSARY QUESTIONS

When a minor implementation decision is unspecified:

choose the best production-ready default.

Document important decisions in:

```text
DECISIONS.md
```

Only require external input when truly blocked by something such as:

* production secret
* external provider credential
* legal/business information

Even then:

continue implementing everything that does not require that credential.

---

# 143. DECISIONS.md

Record important architecture decisions.

Example:

```md
## ADR-001 — Modular Monolith

Decision:
Use NestJS modular monolith.

Reason:
Lower operational complexity for early DevJoo while maintaining clear module boundaries.

Status:
Accepted
```

Do not repeatedly reconsider accepted decisions without new evidence.

---

# 144. DESIGN_SYSTEM.md

Document:

```text
colors
spacing
typography
buttons
cards
inputs
badges
status colors
breakpoints
dark mode
RTL rules
```

Frontend should use shared design tokens.

Do not hardcode random purple values across components.

---

# 145. SEO_PLAN.md

This is extremely important.

Maintain:

```md
# SEO Goals

# Primary Keyword Clusters

# Project Pages

# Category Pages

# Skill Pages

# Hire Pages

# Blog Strategy

# Internal Linking Strategy

# Indexing Rules

# Noindex Rules

# Canonical Rules

# Structured Data

# Sitemap Strategy

# Performance

# Current SEO Tasks

# Completed SEO Tasks

# SEO Technical Debt
```

Update this throughout the project.

---

# 146. DATABASE.md

Document:

* tables
* relationships
* indexes
* data ownership
* important enums
* money handling
* search indexing
* deletion strategy

Update whenever important schema architecture changes.

---

# 147. API_STATUS.md

Maintain API implementation status.

Example:

```md
## Projects

[x] GET /projects
[x] GET /projects/:id
[x] POST /projects
[x] PATCH /projects/:id
[ ] DELETE /projects/:id

## Proposals

[x] Submit
[x] List employer proposals
[ ] Withdraw
```

This prevents future sessions from rebuilding existing APIs.

---

# 148. PROJECT QUALITY RULE

Never optimize for:

> fastest possible generated code

Optimize for:

> a maintainable product that can become a real Iranian technology marketplace.

Avoid overengineering, but do not create throwaway architecture.

---

# 149. UX PHILOSOPHY

Every major flow should satisfy:

```text
Clean
Fast
Smart
Trustworthy
```

The user should always know:

```text
Where am I?
What can I do?
What happens next?
```

---

# 150. FINAL PRODUCT EXPERIENCE

When a freelancer opens DevJoo, the ideal experience is:

```text
Open website
↓
Immediately see real projects
↓
Search/filter without login
↓
See projects relevant to skills
↓
Login in seconds
↓
Set 3 main skills
↓
See DevJoo Match scores
↓
Apply without buying tokens
↓
Know whether employer viewed proposal
↓
Build reputation over time
```

Employer experience:

```text
Open website
↓
Post project easily
↓
AI optionally helps structure requirements
↓
Receive a limited number of relevant proposals
↓
See verified specialists
↓
Compare candidates
↓
Invite matching specialists
↓
Hire confidently
```

---

# 151. CORE DEVJOO MESSAGE

DevJoo must communicate this philosophy throughout the product:

```text
Quality over proposal quantity.
```

For freelancers:

```text
پروژه مناسب مهارتت؛ بدون جنگ بین صدها پیشنهاد.
```

For employers:

```text
متخصص مرتبط پیدا کن؛ نه ده‌ها پیشنهاد بی‌ربط.
```

---

# 152. FIRST EXECUTION INSTRUCTION

When this Master Prompt is provided for the first time:

DO NOT immediately start randomly creating pages.

Perform these steps:

1. inspect the current repository
2. determine whether this is a new or existing DevJoo repository
3. if new, create the Monorepo foundation
4. create all project memory files
5. create the full `TODO.md`
6. create architecture documentation
7. create SEO strategy documentation
8. create the database architecture
9. initialize frontend/backend infrastructure
10. run lint/typecheck/build
11. update `PROJECT_STATE.md`
12. continue with the first unchecked roadmap task

If the repository already exists:

DO NOT bootstrap again.

Read the state files and continue.

---

# 153. CONTINUATION COMMAND

Whenever I later say something like:

```text
continue
continue DevJoo
go ahead
next
ادامه بده
```

you must:

```text
1. Read AGENTS.md
2. Read PROJECT_STATE.md
3. Read TODO.md
4. Identify the current unfinished task
5. Continue implementing it
```

Do NOT ask me:

```text
What should I build next?
```

when the answer already exists in the project checklist.

Do NOT start DevJoo from scratch.

---

# 154. BUG FIX COMMAND

If I report a bug:

1. read project state
2. inspect only the relevant area
3. reproduce or reason about the bug
4. fix the root cause
5. add/update test where appropriate
6. confirm existing behavior was not broken
7. update project state if meaningful

Do not rewrite unrelated modules.

---

# 155. SEO-FIRST RULE FOR EVERY NEW PUBLIC PAGE

Whenever creating a new publicly accessible page, answer internally:

```text
Should this page be indexed?
What is its search intent?
What is its canonical URL?
What should its title be?
What should its meta description be?
Does it need structured data?
How will Google discover it?
What pages should link to it?
What should it link to?
```

If these questions are not answered, the public page is not finished.

---

# 156. FINAL INSTRUCTION

You are not building a generic coding exercise.

You are building:

# DevJoo

a real Persian SEO-first technology and digital freelance marketplace intended to compete for organic search traffic and solve real problems experienced by freelancers and employers.

Treat SEO, architecture, speed, Persian UX, trust, data integrity and long-term maintainability as first-class requirements.

Never restart unnecessarily.

Never fabricate data.

Never mark incomplete work as complete.

Always continue from the project checklist.

Implement incrementally.

Test your work.

Keep the project state updated.

Build DevJoo as a real product.
